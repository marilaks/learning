//pointers basics
#include<iostream>
using namespace std;

int main(){
	int x = 10;
	int *p;//declaring integer pointer
	p = &x; //storing the address of x to p
	
	//address of p variable
	cout<<"address of p "<<&x<<endl;
	cout<<"address of p "<<&p<<endl;
	
	//address stored in p var
	cout<<"address stored in p"<<p<<endl;
	
	//access value using pointer
	cout<<"accessing value "<<*p<<endl;
}
/*freecodecamp
int main(){
	//decalring a variable
	int a = 10;
	
	//decalare pointer to it
	int* p;
	
	//initializing the pointer 
	p = &a;
	
	cout<<"a = "<<a<<endl;
	cout<<"address of a "<<&a<<endl;
	cout<<"p = "<<p<<endl;
	cout<<"address of p "<<&p<<endl;
	cout<<"size of a "<<sizeof(a)<<endl;
	cout<<"size of p "<<sizeof(p)<<endl;
	cout<<"address of p+1 "<<&p+1<<endl;
        //we have not initialized so it wil be 0 at p+1
	cout<<"value at address of p+1 "<<*(p+1)<<endl;

}
*/


